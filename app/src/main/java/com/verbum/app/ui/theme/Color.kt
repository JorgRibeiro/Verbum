package com.verbum.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────────────────────────────────────
// Paleta de Cores do Verbum — Design System
// ─────────────────────────────────────────────────────────────────────────────

// ── Accent: Índigo Vibrante ──────────────────────────────────────────────────
val VerbumIndigo       = Color(0xFF7C6AF7)  // Cor de destaque principal
val VerbumIndigoLight  = Color(0xFF9B8FF9)  // Hover / estados selecionados
val VerbumIndigoDark   = Color(0xFF5A48D4)  // Pressionado / ripple
val VerbumIndigoMuted  = Color(0x267C6AF7)  // Background de chips / badges (15% alpha)

// ── Secundário: Teal ─────────────────────────────────────────────────────────
val VerbumTeal         = Color(0xFF4ECDC4)  // Cor para elementos de idioma / tradução
val VerbumTealLight    = Color(0xFF7EDDD7)
val VerbumTealMuted    = Color(0x264ECDC4)  // Background de badges de idioma (15% alpha)

// ── Superfícies ──────────────────────────────────────────────────────────────
val VerbumBackground       = Color(0xFF0D0D0F)   // Fundo do Bottom Sheet
val VerbumSurface          = Color(0xFF141417)   // Cards internos
val VerbumSurfaceVariant   = Color(0xFF1E1E26)   // Chips, caixas de exemplo
val VerbumSurfaceElevated  = Color(0xFF1A1A22)   // Elementos elevados

// ── Texto ─────────────────────────────────────────────────────────────────────
val VerbumOnBackground     = Color(0xFFF0EFF5)   // Texto principal (quase branco)
val VerbumOnSurface        = Color(0xFFE8E7F0)   // Texto de cards
val VerbumOnSurfaceMedium  = Color(0xFFB0AECB)   // Texto secundário (cinza-lilás)
val VerbumOnSurfaceLow     = Color(0xFF6B6982)   // Labels, seções, dicas

// ── Semântica ─────────────────────────────────────────────────────────────────
val VerbumError    = Color(0xFFFF6B6B)  // Erros (vermelho coral)
val VerbumSuccess  = Color(0xFF6BFFB8)  // Sucesso (verde-menta)
val VerbumWarning  = Color(0xFFFFD166)  // Alertas (âmbar)

// ── Estrutura ─────────────────────────────────────────────────────────────────
val VerbumDivider     = Color(0xFF252530)  // Linhas divisórias
val VerbumDragHandle  = Color(0xFF45454F)  // Drag handle do Bottom Sheet

// ── Extras para MaterialTheme ─────────────────────────────────────────────────
val VerbumSecondaryContainer = Color(0xFF0E2422)
